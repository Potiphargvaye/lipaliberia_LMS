{
    "$schema": "https://json.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
   "railway-deploy": "npm install && npm run build && php artisan config:clear && php artisan cache:clear && php artisan view:clear && php artisan key:generate --force && php artisan storage:link || true"
},
    "devDependencies": {
        "@tailwindcss/forms": "^0.5.10",
        "alpinejs": "^3.4.2",
        "autoprefixer": "^10.4.21",
        "axios": "^1.8.2",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^2.0.0",
        "postcss": "^8.5.6",
        "tailwindcss": "^3.4.0",
        "vite": "^7.0.4"
    },
    "dependencies": {
        "tinymce": "^8.0.2"
    }
}
